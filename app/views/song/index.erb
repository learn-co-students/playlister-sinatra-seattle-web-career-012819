<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>All the Songs!</h2>
    <% @songs.sort_by { |song| song.name }.each do |song| %>
      <a href="/songs/<%= song.id %>"><%= song.name %></a> <br>
    <% end %>
  </body>
</html>
