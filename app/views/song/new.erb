<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/songs" method="post">
      <label for="Name">Song Name: </label>
      <input type="text" name="song_name" id="Name"> <br>

      <label for="Artist Name">Artist Name: </label>
      <input type="text" name="artist_name" id="Artist Name"> <br>

      <% @genres.each do |genre| %>
        <label for="<%= genre.name %>"><%= genre.name.capitalize %> </label>
        <input type="checkbox" name="genres[]" id="<%= genre.name %>" value="<%= genre.id %>"> <br>
      <% end %>

      <input type="submit" value="Create">
    </form>
  </body>
</html>
